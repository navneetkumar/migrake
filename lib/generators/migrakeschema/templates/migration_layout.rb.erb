class CreateMigrakeTable < ActiveRecord::Migration
    def self.up
        create_table "rake_tasks", :force => true do |t|
            t.string "task_name"
            t.timestamp "invoked_at"
        end
    end

    def self.down
        drop_table "rake_tasks"
    end
end